[pytest]
# Project root has no setup.py / pyproject.toml — without pythonpath the
# `from app...` imports in tests/ can't resolve, and pytest exits with a
# ModuleNotFoundError during collection.
pythonpath = .
testpaths = tests
